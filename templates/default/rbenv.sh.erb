export RBENV_ROOT=<%= @rbenv_root %>
<% if @system_ruby %>
export PATH=$RBENV_ROOT/bin:<%= @system_ruby %>:$PATH
<% else %>
export PATH=$RBENV_ROOT/bin:$PATH
<% end %>
source $RBENV_ROOT/completions/rbenv.bash
eval "$(rbenv init -)"
